# Embedding model cache
.fastembed_cache/

# redqueen evolution outputs / extracted prompt artifact
runs/
redqueen-exec-prompt.md

# Python bytecode
__pycache__/
*.pyc

# Local Killhouse runtime artifacts and overrides
.killhouse/*
!.killhouse/config.example.json
# Explicit line kept for the self-hosting validator (bin/killhouse_validate.py --check docs-sync);
# redundant with .killhouse/* above but documents the contract.
.killhouse/config.local.json

# Local agent/runtime artifacts
.flocode/

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Local Python environments and coverage
.venv/
.coverage
htmlcov/
